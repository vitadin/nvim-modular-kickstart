-- User Configuration File
-- This file contains YOUR personal settings for optional features.
-- It is NOT tracked by git, so you can safely git pull without conflicts.
--
-- SETUP INSTRUCTIONS:
-- 1. Copy this file: cp lua/user-config.lua.example lua/user-config.lua
-- 2. Edit lua/user-config.lua to enable/disable features
-- 3. Restart Neovim
--
-- The lua/user-config.lua file will be ignored by git, so your settings
-- are preserved when you update the repository with 'git pull'.

return {
	-- ============================================================
	-- OPTIONAL FEATURES
	-- ============================================================
	-- Set to true to enable, false to disable

	-- LaTeX Support (VimTeX + texlab LSP)
	-- Provides: LaTeX editing, compilation, PDF preview with SyncTeX
	-- Prerequisites: LaTeX distribution (texlive/mactex), Skim (macOS) or Zathura (Linux)
	-- Documentation: docs/tutorial/12-latex-setup.md
	latex = false,

	-- ============================================================
	-- NEOVIDE CONFIGURATION
	-- ============================================================
	-- GUI-specific settings (only applies when using Neovide)

	neovide = {
		-- Font configuration
		-- Format: "Font Name:hSIZE" (e.g., "Fira Code:h19")
		-- Common fonts: JetBrainsMono Nerd Font, Fira Code, Hack, Menlo, Monaco
		-- Leave as nil to use Neovide's default system font
		font = nil,

		-- Font size (alternative to specifying in font string)
		-- If set, uses Neovide's default font with your custom size
		-- Leave as nil to use default size
		font_size = nil,

		-- Examples:
		-- Option 1: Specify complete font with size
		-- font = "JetBrainsMono Nerd Font:h18",

		-- Option 2: Just change size (keeps Neovide's default font)
		-- font_size = 20,

		-- Option 3: Specify font without size, then override size
		-- font = "Hack",
		-- font_size = 16,  -- Uses Hack at size 16
	},

	-- ============================================================
	-- FUTURE OPTIONAL FEATURES
	-- ============================================================
	-- Add more optional features here as the repository grows
	-- Examples:
	-- markdown_preview = false,
	-- git_integration = false,
	-- debugging = false,
}
